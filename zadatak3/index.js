// Potrebno je napraviti algoritam koji ce vratiti niz sa koordinatama
// kojima je potrebno ici da bi se najbrze stiglo od startCoordinate
// do endCoordinate bez prolazenja kroz block polja.

// Takodje, potrebno je napraviti algoritam koji ce da generise block polja
// tako da su ona random, ali da nikako ne zatvori startCoodinate polje i
// endCoordinate polje.

const config = require('./config.js');
const blockList = [];

const generateBlocks = () => {
	for (let j = 0; j < 10; j++) {
		
	}
};

console.log(config.startCoordinate);
console.log(config.endCoordinate);
console.log(config.blocks);